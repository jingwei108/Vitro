# MoonBit 迁移蓝图 v1

> **日期**：2026-09-18 ｜ **性质**：决策文件（第 14 号汇总代理产出，只读勘察基础上编写，未执行任何代码改动、未 git 提交）
> **输入**：13 份模块勘察报告（同日，`MoonBit迁移_*模块勘察报告20260918.md` ×12 + `reports/MoonBit重写_知识沉淀模块勘察报告_2026-09-18.md`）+ [评估报告](MoonBit迁移方案评估报告20260918.md)
> **上位约束**：与评估报告 §7 四道证伪门（门 0 LLM 效率 / 门 1 VM 热循环 / 门 2 Wasmtime / 门 3 快照往返）与 §8 假设清单 A1–A9 冲突时，**以评估报告为准**；本蓝图在其之下细化。
> **形态裁定（已由项目所有者拍板，2026-09-18）**：① 同仓绞杀者渐进迁移——Rust 版冻结不删、降级为差分对照 oracle，MoonBit workspace 与 `native/` 并列；② 不赶 9 月黑客松，按自有节奏迭代，以国家级/国际级赛事与 mooncakes 分片发布为可见性节点。
> **真值口径**：全局数字一律引 `reports/facts.json`（本蓝图读取快照 `generated_at` = 2026-09-18T13:01:41+08:00，`git.rev` = `4b57191`）并标 `as_of`；勘察实测数字为 AS-OF 冻结。
> **修订记录**：v1 初稿基于四路摘要代理的二手信息合成。**2026-09-18 第一轮修订**：项目所有者裁定"实测大于脑测，禁止基于二手摘要下结论"后，13 份报告逐份全文亲读（9/13）。**第二轮修订**：二次批评成立（"读报告仍是读文档"）——补上代码锚点亲验（5 处）+ 引擎探针 8 条亲手复现 + C1 删臂实验终局（两报告各错一半）+ 载体级吞吐首批数字（native ≈1.1×）。**第三轮修订**：**0d 关闭**（release 重建 + replay 61/61 + serve_smoke 57/57 + C3 定案 24MB）、**1a 关闭·通过**（packed 1:3.8 精确）、**1b 首测完成·名义红**（迷你 VM 孪生对照：native 3.67~3.83× / wasm-gc V8 ~7.05×，校验和逐位一致；四项未闭合故判决挂起上决策桌）——**假设 A3 首获实测数据，方向不利，这是迁移评估以来第一个真实红色信号**。附门 0 非正式累积（8 类一手 MoonBit 错误）。逐份复核笔记与三轮实测原始数据见 [MoonBit迁移蓝图v1_第一手复核记录.md](MoonBit迁移蓝图v1_第一手复核记录.md)。待办：门 1 四项闭合、门 0 正式对照、剩余 4 份报告亲读。
> **第四轮修订（同日续）**：**门 1 关闭·通过（条件 A/B 随迁）——判决翻转**。真实引擎锚（`vm_bench` 1k×1k：现役解释器 **512ms** / JIT **55ms**，加速 9.27× 与文档吻合）证明 MoonBit 迷你 VM（wasm-gc/V8 单次 156ms）**快于现役解释器 3.3×、慢于 JIT 2.8×（线内）**；第三轮名义红（孪生对照 3.67~7.05×）系**对照物失真**——理想化 Rust 孪生比现役解释器快 23×。**方法论教训入档：比值结论必须声明分母引擎**。阈值按教学绝对负载校准（≤10M 步 ≤1s）大幅通过；V8 编译占比 ~0.1% 排除；`FixedArray` 无公开去界检原语（优化轮闭合）。**门 1 成为四道门中第一道拿到终局实测数据的门；C10（不搬 JIT）获数据支持**（放弃 JIT 的代价收敛为热循环场景 ~2.8× 且仍快于现役解释器）。条件 A（S6 片性能回归锚，对照 512/55ms 双基线）/ 条件 B（LeetCode 全量 ≤3s）随迁。
> **第五、六轮修订（同日续）**：**门 2 有条件通过**（wasmtime v33 `-W gc` 下 2.34s 真实运行；默认 CLI 拒 GC 模块——部署须开特性；过程抓到两个测量假象：管道退出码 / 哨兵未证红，均已入档升级为硬规则）；**门 3 通过·模块级**（快照往返 PASS 5/5 双目标：全字段 + seek 确定性 + 隔离区三件套 + UAF 无假阴性）；**门 0 通过·弱**（干净上下文子代理：首过 1/6、≤2 轮收敛、6/6 正确含 quirk 保真；三大语言差异进 S1 工程约定）。**四门终记分：0 红、2 通过、1 弱通过、1 有条件通过——"任一门红即回退"未触发，评估报告 §7 阶段 0 由本复核第二至六轮实测实质完成，进入 S0.5/S1 可开工状态。** 逐份复核笔记与六轮实测原始数据见 [MoonBit迁移蓝图v1_第一手复核记录.md](MoonBit迁移蓝图v1_第一手复核记录.md)。
> **第七轮修订（同日续）——v1.1 定稿**：**最后 4 份报告亲读完毕，13/13 全量亲读闭环**。本轮补强 4 处 + 修正 1 处：①C9 补 CS2 复用冲突（砍 C++ 与既定策略冲突，S9 须合并裁定）与短路方案（强化 v1=C only）；②P5 补 4 例手写 golden 循环论证处置与 gap 用例不触红审计；③spike 表新增 U1（认知链二/三/四层零出口——差分退路不存在，阶段 1 前须补 Rust 侧导出）与 U2（U0#7③ 是 SharpTutor 当前阻塞项，与重写开工同批拍板）；④F3 精确化（Bytes 是 FixedArray[Byte] 的 identity 视图，core 源码取证）；⑤capability_flags 19→**17 项**（摘要误传修正）。**至此蓝图所有论断可溯三层之一：报告原文亲读 + 复核二至六轮实测 + 代码亲验。** 报告质量分层结论（实测派 > 亲读派 > 统计归类派）见复核记录 §AA。
> **第八轮修订（同日续）——丢/继承/改进逐项清点**：项目所有者指令"实测大于脑测、统一真相来源"下的逐条复核——**丢弃 9 项、继承 8 项、改进论据 10 项全部亲证成立，0 项推翻**；两处论据加码（`{:?}` mangling 实为 4 处、E 前缀伪造实为 4 处）；数字对账 7 组中 6 组精确一致、1 组由我亲手数决（**StepPayload=14，管线报告 15 误，C2 终局**）；libc 产物以产物自身为真相源对账（3485/88/1000..1087 精确）。方法论新增第五类工具陷阱（反斜杠 grep 排除失效 ×2）。逐项清点表见复核记录第八轮 §表 A–E。

---

## ① 模块勘察结论合并表

| 模块（报告） | 规模实测 | 可复用资产核心 | 抛弃包袱核心 | 本模块 spike 核心 | 最大单点风险 |
|---|---|---|---|---|---|
| shared+ast | 1,781 行 / 2.3%；ErrorCode **137 变体**、Expr 26、Stmt 16、Type 17 | 码位分配表（外部契约）、AST 形态、mangle 规则表、depth 迭代测深 | `as i32` 脱类型 59 处；Type::PartialEq 非自反；serde 派生无消费者；catalog 覆盖 77/136（缺 59） | S2 穷尽 match 收益；S8 码表 Go 脚本生成 | "加码漏改"零编译期信号（0 处穷尽 match） |
| lexer | 3,523 行；**预处理器 1,786 > 词法核心 1,737**；14 条实测缺陷探针（本批证据等级最高：debug 当天构建 + 核实 4b57191..HEAD 零差异） | 宏展开 C99 §6.10.3.1 语义 140 行；include 解析；57 单测；675 golden | `Vec<char>`+splice 架构（MoonBit 无对应物）；哨兵指令；行号补偿；**3 条实测静默错值**（`||` 短路不归一选错分支 / string 侧转义错字节 / `#elifdef` 静默选错分支）+ `#if` 位运算与三目**显式 E1014 拒绝**（非静默错值，此前表述不准）+ `#warning` 零诊断 | S1 字节承载 / S5 宿主 IO 抽象【门禁级】 | **H-5：wasm-gc 无文件系统 → 自定义 include 静默失效** |
| parser | 4,720 行 / 6.2%；parse_* 76 个；回滚点 20 | 优先级瀑布 21 帧；声明符螺旋；错误恢复集；50 黑盒 + 9 崩溃锚 | 7 处人工深度挂点（已被 J1 证伪）；suffix_count 死字段；子流解析 hack | S1 递归栈深【已实测：wasm-gc 854 层 vs Rust 自限 62】 | **J1：声明符类型通道无防护，1200 层通过/1300 崩，活的零诊断崩溃** |
| typeck | 8,816 行；27 可变字段；Pass 13 个 | 隐式转换 12 臂表；mangle 三单源；重载评分；U3 红→绿锚 | `{:?}` mangling 含 SourceLoc（同形不同行 → 不同名）；1024 轮上限按轮数计数；签名真相源 4~5 套冲突；895 行硬编码容器 | S1 Map 迭代确定性（**跨宿主种子差异预期红**）；S6 纯函数改写 vs O(n²) | Bytecode Libc 产物确定性完全依赖 **Go 侧 sort_keys**（Rust 侧 HashMap 键序随机） |
| codegen | 6,237 行；47 字段；opcode 引用 127/132 | R1 内存布局纯函数；bump 单入口 fail loud；调用约定 ABI；libc 产物 3,485 指令 | temp_slot 固定 4 槽（**槽位事故 8 条**，裁定样本 3 被低估）；patch_jump 越界静默；四张平行符号表 | S6 LIFO 槽位分配器 + 8 条事故回归 | **★A 全局/静态 `char* p="hi"` 静默错值（676 用例零覆盖）**、★B gen_struct_copy 槽冲突（五层防线全绿） |
| runtime+vm | 10,901 行 / 14.3%；opcode 132；host funcs **110**（20 被 libc 遮蔽） | 扁平快照 22+11+8 字段 + Full/Delta；UAF 区间算法；bump+隔离堆模型；JIT 模板策略（天然适配 wasm） | 字符串错误码协议；`freed_logs` 依赖有序 range 查询（**正确性依赖**）；每步 1MB 快照（≈100GB memcpy 流量/10 万步） | §7.1 载体 × 访问模式矩阵【门 1 本体，**13 份报告无一份测过吞吐**】 | JIT 与解释器**溢出语义分歧现存未修**（⑥-4，判定性用例已给出） |
| cpp_frontend | 实为容器布局加载器 186 行；C++ 专属 ≈6,514 行 / **8.5%**（Phase 数 26%——"三分之一"口径被实测纠正） | `.cpp` 真源 + JSON 加载器（Phase 41 零硬编码）；类布局算法；RAII 策略；Clang++ 现场对照 99 例 | 字符串命名合成实体 12 种 mangled 名 11+ 文件各自拼接；4 例手写 golden 自证循环；死 token/死 AST 变体 | S1 checked 六算子（Int64 加宽法）；S6 无借用检查器的递归替换 | `A<1+1>` 两行 → 双实例（`{:?}` 键含位置）；4 例 golden 缺失未被防线覆盖 |
| 管线+会话 | ≈5,200 行；capi 45 导出；serve 21 方法 | 三契约（错误帧同构/id 关联/reset 白名单）；多文件行号映射；bundle schema；serve_smoke 57 断言 | 管线双轨 ~340 行×2；`"main.c"` 兜底 54 处；注释腐化 8 处；B15 unified/stream 死代码 | S4 JSON 数字序列化（**1.0→1 实测风险**）；S5 stdin；S6 退出码 0/1 | 一条畸形请求杀死会话（abort exit 1，护栏机制消失） |
| 出口层 | 3,271 行；unsafe 47 处 | E-P1-5 三通道语义（"输出口径宪法"）；21 方法表；StepPayload schema+冻结测试；vitro_capi.h 三类所有权分区 | **45 个 C 导出中 28 个无消费者**；catch_unwind 三层护栏；rust-alloc 所有权契约 | S6 wasm-gc 导出面形状；S7 可恢复性边界清单 | wasm 冒烟 ABI 断言是**空洞断言**（测的是指针非空）；产物体积三方不符 |
| unified | 3,690 行 / 25.9% | StepPayload 14 字段；受控词汇表；版本轨道+tripwire；检查点链不变量 3 红锚 | 每步 1MB 快照（`pre_step_snap` 复用是半修）；Arc 三处 COW 在快照形状下退化；stream 差分编码**无生产调用点** | S2 滚动窗口 GC 行为（持久结构经典空间泄漏）；S10 快照往返 | restore 不重建 `local_sym_map`（与 region_index 同构，义务已违反两次） |
| diagnostics+算法 | 36 文件 8,748 行；卡片 77；图谱 25/25；算法 43 | 五张数据表（文案/概念/模式/路径/载荷）；标注 golden 37 模板 311 条；滑窗/路径组装/图激活机制 | **认知链二/三/四层与补全零出口**（"库代码+单测"非能力）；`infer_semantic_label` 用源码文本反推执行语义（最深架构债）；悬空引用 29 处 | spike 1 @json 规则表；spike 5 三坐标口径 | 检测器裸 contains 误判（isValidBST→BST 插入等，15+ 红锚须逐条搬） |
| scripts+防线 | Go 21 文件 9,922 行；用例 758；golden **733 .out**；`#[test]` 900（native/tests 口径） | 判定树+自检；缓存 schema；六类隐性口径头注；fuzz 5 种子；J9 台账；**裸奔期 24 例候选表 + M-0~M-9 里程碑** | ctypes/DLL 绑定层（唯一不可移植大块）；psapi RSS 口径；3 个活跃 Python 生成器（sync_templates 是唯一 golden 写入者） | S3 字节流归一；S5 顺序确定性 spike 化；S8 stdin 注入 | **golden 缺失静默跳过**（`vitro_e2e.rs:186`，4 个新 C++ 用例正因此未被覆盖） |
| 知识沉淀 | docs 43 文件 17,254 行；FAILURES 12 份 **97 条目**（"218"为表格行口径） | Clang/Clang++ golden 733；制度资产 27 条盘点；红→绿 35 处命中；**差异台账编号化草案 + 19 项 capability_flags** | 文档漂移 K1~K5（三份数字三套说法）；伪全绿 M1~M9；ABI 版本化制度随 capi 作废 | S7 包依赖工具检查；S8 golden 夹具承载 | 差异台账分散 3+ 处约 30 条互相矛盾——机器单源化的最强论据 |

---

## ② 全量 spike 清单（去重合并）

**已实证事实（13 份报告现场实测，无需重测，直接采信）**：

| # | 事实 | 证据来源 |
|---|---|---|
| F1 | MoonBit `Int` 四则**静默回绕**（`2147483647+1 → -2147483648`），全 core **无 checked/overflow/wrapping API** | cpp 报告 §7 F1（core 源码取证 + 全库扫描） |
| F2 | `String` 内部 UTF-16，`length()` 数 code unit（`"中文".length()=2`、`@utf8.encode().length()=6`、`to_bytes()` 已 deprecated） | parser S5 + 管线 S2 两路独立实测 |
| F3 | `Bytes` 不可变（**且是 `FixedArray[Byte]` 的 `%identity` 视图**——core 源码取证，对原数组的修改会反映到视图）；`FixedArray[Byte]` 可写（`arr[3]=b'A'`→65） | 管线 spike 2 + cpp F3（core 源码直读取证） |
| F4 | 递归下降栈深：21 帧/层瀑布下 wasm-gc **854** 层 / js 538 / native 1005（崩溃 `0xC00000FD` 不可捕获） | parser S1 |
| F5 | wasm-gc 产物 imports 仅 `spectest.print_char`（回调收 Unicode **码点**，宿主须做码点→UTF-8 编码）、exports 仅 `_start`；`@env` 在 wasm-gc **无宿主实现**（含 `rand_internal` 返 false、无熵源） | scripts S1/S2 |
| F6 | Hasher 默认种子：wasm/wasm-gc = 0，native/llvm/js = 随机；`HashMap::iter` 序 unspecified；`LinkedHashMap` 可用 | typeck S1 预判（core 源码取证） |
| F7 | `Int::MAX+1` 回绕、`1<<1000=256`（位移 mod 32）、`x/0` → RuntimeError trap、`7%-2=1` | parser S6 |
| F8 | `moonc v0.10.13` 存在跨文件顶层 `pub let` 引发的 link-core 阶段 ICE（常量内联即消失）——工具链不稳定实证信号 | parser S8 |
| F9 | JSON 数字序列化 `1.0→1`、`-0.0→0`（与 serde_json 形状不同）——**产物逐字节 diff 的头号风险** | 管线 S4 |

**待测 spike（按执行顺序；门禁级 ★）**：

| 序 | spike | 最小验证程序 | 判定标准 | 预计 | 失败后果 |
|---|---|---|---|---|---|
| 0a★ | ~~门 0 LLM 效率~~ | **已关闭·通过（弱）（第六轮实测，复核记录 §U）**：干净上下文子代理、6 个真实函数、零文档纯报错驱动——首过率 **1/6**、每函数 ≤2 轮修复、**收敛后 6/6 语义全对**（含 quirk 保真与饱和边界断言）；"重写周期不可估"不成立；三大语言差异（derive(Eq) 位置 / mut 半颠倒 / 带参构造器非一等值）写入 S1 工程约定 | — | — | — |
| 0b | 环境前置 | node 在 PATH（lexer 报告 `moon run -e` ENOENT 教训）；`moon run cmd/main` 冒烟 | 全绿 | 10 分钟 | 修复环境后再开工 |
| **0c** | ~~口径冲突消解（C1）~~ | **已闭合（第二轮亲测，复核记录 §C）**：删臂实验 check/build 均默认 error，C1 终局裁决见 §3.5 | — | — | — |
| **0d** | ~~Rust 侧 release 重建前置~~ | **已关闭（第三轮实测，复核记录 §F）**：release 重建含 HEAD → replay **61/61** + serve_smoke **57/57** + C3 定案（当轮实测提交峰值 **24MB**） | — | — | — |
| 1a★ | ~~门 1 载体 packed~~ | **已关闭·通过（第三轮实测，复核记录 §G）**：100M 元素外部 RSS 采样——`FixedArray[Byte]` 106.9MB vs `FixedArray[Int]` 406.9MB（精确 1:3.8）⇒ **packed 成立（1 字节/元素）** | — | — | — |
| 1b★ | ~~门 1 吞吐~~ **已关闭·通过（条件 A/B 随迁）（第四轮实测，复核记录 §L-§O）**。终局数据：真实引擎锚 `vm_bench` 1k×1k——现役解释器 **512ms** / JIT **55ms**（加速 9.27×，期望锚过）；MoonBit 迷你 VM（20 op + 完整检查链，校验和同口径）单次 **native 81ms / wasm-gc·V8 156ms** = **快于现役解释器 3.3×、慢于 JIT 2.8×（线内）**；第三轮的"名义红 3.83×"系对照物失真（理想化 Rust 孪生比现役解释器快 23×）；阈值按教学绝对负载校准（≤10M 步 ≤1s）大幅通过；V8 编译占比 ~0.1% 排除 | **条件 A（随 S6 片）**：真实引擎每指令开销 ≤ 迷你 VM ×3（性能回归锚，对照 512/55ms 双基线）；**条件 B（随 C10 复核）**：LeetCode 全量复跑延迟 ≤3s | — | — | — |
| 1c | 溢出检测成本 | `to_int64()` 中转 + 范围判定的 ns/op，占每条算术指令比例 | 与 Rust `arithmetic.rs` i64 中转同构、可接受 | 半天（并入 1b） | F1 无 checked API ⇒ 这是唯一路径，成本超标则门 1 连带评估 |
| 3★ | ~~门 3 快照往返~~ | **已关闭·通过（模块级）（第五轮实测，复核记录 §Q）**：MoonBit 实现快照/恢复——全字段断言（1MB 逐字节 + freed 全表 + 隔离区三件套 + 标量演化）+ seek(5) 重放确定性等价 + UAF 无假阴性，**PASS 5/5（native 与 wasm-gc 双目标）**；真实引擎集成版（全字段 + 派生索引自动重建）留 S6 片验收 | — | — | — |
| 2 | ~~门 2 Wasmtime~~ | **已关闭·有条件通过（第五轮实测，复核记录 §R）**：零 import 产物在 wasmtime v33 **`-W gc` 下 2.34s 真实运行**（哨兵双证红）；**v33 CLI 默认拒绝 GC 模块**（解析即败）⇒ 服务端宿主形态保留，部署须显式开 GC 特性；过程抓到两个测量假象（管道退出码 / 哨兵未证红）已入档 | — | — | — |
| **U1** | **认知链二/三/四层与补全的差分退路（第七轮亲读补入）** | diagnostics 报告 8.2 实测：knowledge_graph / misconception / learning_path / completion **在 Rust 侧零出口**——"趁 Rust 版仍在做差分扫描"这条唯一不重踩坑路径**对它们不存在** | **阶段 1 之前**在 Rust 侧补最小导出（serve 加 4 方法或 `diagnostics_probe`）+ 用例外置 JSON | 缺口不补则 S8 片迁移无等价性证据 |
| **U2** | **U0#7③ 的下游时效（第七轮亲读补入）** | 管线报告 I1：`vitro_capi.h` 补 19 声明是 SharpTutor **当前阻塞项**——砍 capi 后对象消失 | 该项与"重写是否开工"**同批拍板**，不得默认搁置；若重写延期而下游催办，先做止血 | 下游义务违约风险 |
| H1 | 有序映射（freed_logs 等价） | 16,387 条不重叠区间：插入 / `range(..end)` 降序扫描 + 提前 break / 部分重叠裁剪 | 有序容器或自建排序数组+二分，算法正确性与 ns/op 达标 | 1 天 | **正确性依赖**：普通 hash map = 静默漏检 UAF |
| H2 | 不可变共享（A5/评估假设） | ①改一个标量字段后 1MB 数组不被复制；②`heatmap` 每步更新形态的成本；③滚动窗口持久序列的空间泄漏（100 万步 RSS 斜率 ≤64B/步） | 三项全过 | 1 天 | `heatmap` 重设计（分片计数）；"不可变天然解决增长"论放弃 |
| H3 | 退出码 + stdin（CLI 形态前提） | `@sys.exit` 在 wasm-gc 可用性；stdin 逐行读不吞换行；流式 flush | 三项成立或给出降级形态（预编译 wasm + Node 宿主 bridge） | 半天 | serve/CLI 形态改变，驱动契约重定义 |
| H4 | catch 边界 | abort / 越界 / 除零能否被 try/catch 捕获（三项） | 产出"可恢复/终止"清单，写入协议设计前提 | 半小时 | 协议层全路径防御（每方法级 Result） |
| H5 | 大 JSON 嵌入 | 563KB bytecode_libc_data 构建期嵌入，wasm 体积增量 | 不显著膨胀（现冒烟 3.75MB 基线） | 半小时 | 退化方案：构建期生成 .mbt 常量（有 7,438 字节先例） |
| **H6★** | **宿主 IO 抽象**（lexer S5 门禁级，本轮亲读补入） | `trait SourceProvider` 两实现（存根表 / 真实 fs），wasm-gc 目标下 `#include "local.h"` | ①wasm-gc 构建通过且自定义 include 给出**显式诊断**（"宿主不支持文件系统"），**不得静默失效**；②native 正常；③错误可区分"文件不存在"与"宿主不支持" | 半天 | **H-5 复现且无法修复——与单出口目标直接冲突**（wasm-gc 下 `@env` 全部 import 无宿主实现，F5 实证） |
| M1 | 词法吞吐（lexer S6） | ≥100KB 源（中文注释+数百宏+include 链）tokenize×100 vs Rust | 慢 3× 内可接受 | 半天 | >10× 重评策略 |
| M2 | 诊断收集不中断（lexer S7） | 10 处错误源走完整 lexer | 10 条全收集、顺序确定 | 2 小时 | 错误恢复策略重设计 |
| **M5** | **顺序确定性 spike 化**（scripts S5，本轮亲读补入） | 并发跑 N 用例 + 注入"乱序完成"休眠；构造不回填索引的对照实现 | **门禁必须在乱序完成下仍正确**——对照实现必须报红（P1"结果错配门禁仍绿"事故的 spike 化） | 半天 | 并行驱动骨架不可信 |
| M3 | `Bytes` 的 O(1) 头部截除（VM spike-10④） | 有界环形 + 部分截除语义在 `Bytes`/可变累积器上的表达 | OutputLog 共享方案成立 | 半天 | OutputLog 快照共享重新设计 |
| M4 | FuncRef/闭包成本（VM spike-5） | 顶层函数入数组 10^7 次间接调用 vs 直接调用；闭包捕获是否引入分配 | JIT 若保留：间接成本可接受；若不可接受 → enum 分派（已是建议形态） | 半天 | 仅影响 JIT 裁定（倾向性已不搬） |
| M6 | `@json` 规则表加载（diagnostics spike 1） | 真实 export_json 产物 → `Map[Int,Card]` | 77 条全解析、缺 key 返 None 非 default | 半天 | 数据外置方案调整 |
| M7 | `.mbtx` 重放对账（unified S7） | 子进程 NDJSON 双向管道 + 中文汇总输出 | 四判据全过（双向流式/stdout 字节可控/--wasm-policy 授权/与 Go 版逐行一致） | 半天 | 保留 Go 驱动，.mbtx 只做片内工具 |

**spike 依赖序**：0a/0b/0c → 1a → 1b(含 1c) → 3；H1/H2/H3 可与门并行；门 2/M 系列随后。**任一门红即整份回退（评估报告 §7 原文）**；spike 代码一律放仓库外独立目录，不触碰 `native/`。

---

## ③ 在途工作接纳矩阵 + 冲突裁决

### 3.1 迁移前必须在 Rust 侧修复（否则差分锚点"两侧一致地错"或缺陷带病迁移）

| # | 条目 | 依据 | 验收 |
|---|---|---|---|
| P1 | **parser J1 声明符类型通道栈溢出**（活的零诊断崩溃）——**第二轮亲手复现（复核记录 V2）**：release 1300 层崩证实时；**但边界是二进制相关的**：报告的"1200 通过/1300 崩"是 release 专属，debug 实测 1300/1400 通过、1500 才崩。锚点必须钉二进制，或修复后改形状锚 | parser 报告 §4-A1 | 深度预算覆盖 Type；**锚点钉 release 二进制**（或以修复后"同形状确定性诊断"为锚，附两二进制阈值对照） |
| P2 | **codegen ★A 全局/静态字符串指针静默错值**（本轮亲读精化：根因是**双侧**的——codegen `lib.rs:392-397` 不校验目标类型 **且** typeck 全局标量初始化不插隐式转换（`vitro_typeck/lib.rs:325-352` 只 `check_assignable`，而局部路径 `decl.rs:288/321` 会 `insert_implicit_cast`）） | codegen 报告 §④（676 用例零覆盖，regex 实测命中 0） | 修复两侧：codegen 目标类型判据 + typeck 全局/局部对称化；先 clang 实跑确认 golden `[hi]`（报告附录 C 自认该项是"C 语义直判，clang 实跑未做"）；baseline `global_string_pointer.c`/`static_string_pointer.c` 红→绿 |
| P3 | **E 前缀诊断缺陷**（W/H 码打 E 前缀，三面复现） | shared/ast M-3/M-5 + 管线 I11 | E3 诊断帧锚点可建 |
| P4 | **string 侧转义与 char 侧收口**（\x4→3、A\012B→6 静默错值） | lexer ⑥-5 + shared/ast M-1 | 非 ASCII/\xHH 用例进锚点集（现覆盖 0） |
| P5 | **golden 缺失静默跳过修掉**（`vitro_e2e.rs:186`）+ 补 4 例 C++ golden + 补 ★A 判定性用例；**本轮亲读补充三条**：①补 golden 前先核对生成口径（scripts P10 实测：`sync_templates.py` 生成时前置注入三个头 + 不喂 stdin，与 shadow 消费口径不同——`.out` 只作第二来源，live clang 为主真值）；②五个 `KNOWN_*` 常量一律成对（跳过 + 反向"转绿即 panic"，现 `KNOWN_BASELINE_COMPILE_FAILURES` 缺反向测试）；③**4 例手写 golden（`cpp_vitro_vec_class` 等）是 Vitro 自证循环论证**（内容与 Vitro stdout 逐字节相同而 Clang++ 编不过）——迁移对账时改为 std::vector 等价物取真对照，或显式登记"无独立 oracle、仅回归锚"；另 **gap 用例差异不触红**（7 条 gap 的回归不 exit 1）须逐例审计（对齐 C 侧 J2 闭环先例） | scripts ④-2/④-4 + cpp 8.3/6.10 | 缺 golden 必红；83=79 差集清零；反向锚入位；4 例手写 golden 处置裁定 |
| P6 | 列号口径双坐标冻结（M-13 形态用例固化：`int main(){ int "中文"; }` 报 13 应 17——**解析路径偏差而词法路径自洽**，两路径的区分本身是关键实测发现） | shared/ast M-13 + lexer ⑥-1（双报告独立复现） | 锚点用例入库 |
| **P7** | **AST/符号表 dump 出口在 Rust 侧新建**（本轮亲读补入：typeck 8.1 与 parser 8 **双报告独立确认**——全仓 `dump_ast` 零命中，AST 已有 27 处 serde 派生但**无 dump 入口**；E1/B 级锚点的硬前提，必须趁 Rust 版还在时落地） | typeck 8.1 / parser 8 / shared/ast ⑨ | `--dump-ast` 出口 + Go canonicalizer 可跑通 |

### 3.2 直接按目标架构实现（不在 Rust 侧修，迁移时一次做对）

单态化纯函数化 + 实例化缓存（typeck §⑤-1，含 1024 上限三处改法：按栈深/带 SourceLoc/点名模板）；`SourceLang` 单源化（46 处布尔穿透，typeck 逐文件清单为准）；**预处理独立 pass + LineMap + 双坐标**（lexer ⑤-1/⑤-2，项目自裁的四类问题共同根因）；Layout Planner 纯函数（codegen）；**LIFO 作用域化槽位分配器**（U3#1，8 条槽位事故回归必挂）；U3#3 局部偏移回收；U3#7 变参动态预留；统一写路径校验器 `checked_access` 单入口（host 层绕过检测结构性消失）；`freed_logs` 有序数组+二分；OutputLog 载体 Bytes 化；VFS 纳入快照；诊断结构化（code+span+severity，先双轨一版）；note 通道有界化；U6#4（状态收进引擎、`Execution{vm,engine}`）；**Trap 回退改"检查点+正向重放"**（unified 5.1，消每步 1MB 快照）；stream 差分编码升格窗口内存表示（unified S1 三选一取 b）；语义标注从行文本启发改执行事件（diagnostics ⑤-4）；mangled 名单源 `vitro/names` 包（typeck §⑤；`{:?}` mangling 换 A7 式规范化编码，验证法 `A<1+1>` 不同行观察双实例）；签名真相源单表（O6，printf/putchar/strcpy 三处实测冲突）；容器数据驱动吃掉 895 行（O11："零硬编码"仅覆盖 POD 路径）；词汇/规则/文案五张表外置 JSON；错误码穷尽 match + Go 生成脚本（shared S8）；**golden 生成器换由 Node/wasm 宿主驱动承担**（本轮亲读补入：`sync_templates.py` 是唯一 `cases_golden` 写入者且为 Python，迁移期 golden 重算必须能跑通——scripts B6）；**`memory.regions` 字段在新契约当场定型**（出口层 ④-3 最高优先级：capi 第二批已取消，"过渡形态"就是终态，字段清单见出口层 M20）；**"喂入不重置运行态"与"会话级配置不被初始化覆盖"两条不变量**（出口层坑 6/7：waiting_input 粘滞位、setup_vm 曾硬编码 10M 步抹配置——均与语言无关）；**发布缓冲显式状态类型 `Empty|Pending`**（unified 坑 7：滞后一帧语义只存在于注释）；`Stmt::Try` 保留标 reserved-for-csharp（shared/ast B6）。

### 3.3 放弃并记录理由

capi 全部后续批次（45 导出义务、U0#7 ④之外、第二批 4 函数改 serve 方法、第三批拆半）；双轨管线 ~340 行×2；unified/stream 死代码（427+231+264+43 行）；K&R 定义语法；`-I`/系统搜索路径；H-4 存根硬遮蔽；D14（已清零，销项留痕）；D16"再拆"（改"每包公开接口面"约束）；诊断切面 38/810 与 margin 门禁（迁移期以差分锚替代）；`vm_benchmark.rs`；trace 死路径（`__vitro_step` 零发射）；`global_count`/`OpCode::Strlen` 死路径（或显式登记"产物可用"）；`vitro_get_jit_stats` 统计虚高 2× 随 JIT 裁定处置。

### 3.4 待证后再定

A-1（JIT 溢出分歧实际输出：`run` vs `unified` 判定性用例）；lambda 双 E3014；U4 编号空洞；W5 零引用变体两种口径（123/137 vs 11/77 vs 登记 ~36）；`quarantine_budget` 是否随快照往返（A-5：先裁定"会话配置 vs 执行状态"分区）；Bytecode Libc 机制形态（固定索引段 vs 内建原语——与 JIT/C++ 同批 S9 裁定）。

### 3.5 跨报告冲突裁决（13 处，逐条）

| # | 冲突 | 裁决 |
|---|---|---|
| C1 | partial_match 默认级别：shared/ast（check 默认警告，须 -d）vs parser（实测构建失败默认即 error）——**第二轮亲测定案（2026-09-18，复核记录 §C）**：5 变体 enum 删臂实验，`moon check` 与 `moon build` **均默认即 error**（"Failed with 5 warnings, 1 errors"，exit 127）；且诊断**列出缺失变体名**（"some hints: E"，parser"hints 为空"在 5 变体 case 未复现，116 变体是否截断待复核）。**shared/ast 0-4 被证伪、parser 证实** | 终局：CI 接线用 `moon check` 即可（`-d` 非必需，可作全警告升级选项）；spike 0c **闭合** |
| C2 | StepPayload 字段数 14（schema/unified）vs 15（管线）——**第八轮亲数终局（复核记录 §表 D）**：`unified/types.rs:16-31` 亲手数 = **14 个字段**（step_index…root_cause_hint）；**管线报告的 15 误**（疑混数 AutoStepResult） | **14 定案**；管线报告该条登记为勘误 |
| C3 | RSS 峰值 23MB（main.go 注释）vs 24MB（ci.yml/AGENTS.md）——**亲读后修正裁决**：出口层附录 12 确认**两值均无 facts key，属文档陈述冲突【待证】**，此前"实测 24MB 为准"的说法不成立（24MB 同样是文档值） | 以**当轮 serve_smoke 报告产物**为准（facts 只认带 schema 的产物）；两处文档值冲突回填（进 P5 同批）；Node 宿主形态下基线本就要重标定（unified M2） |
| C4 | native/src/ 行数 14,267 vs 14,324 | 口径差异（空行/文件集）；蓝图不以此为判据 |
| C5 | 用例文件数 758（scripts 实测）vs"1,888 份量级"（codegen） | 以 758 用例文件 + 733 golden 为准；1,888 推测为全目录文件（含 .h/.in/.out），标待证 |
| C6 | CPP_FAILURES"约 40"vs 实测 20 条人工归类 | 97 `###` 条目 / 218 表格行 / 20 人工归类是三个口径，全真；"40"为旧文档错误回填 |
| C7 | facts 快照 01:36 vs 13:01（同 rev） | facts 是滚动文件，统一引 13:01:41 快照 |
| C8 | stream 差分编码：unified"无生产者三选一" vs 出口层列为验收锚 | 采纳 unified：锚点降级为往返单测（库语义）；迁移后按 5.2 升格内存表示复活为新生产者 |
| C9 | C++ 排期：评估 §3.2"阶段 0 前拍板" vs §7"阶段 2 单独裁定" | **本蓝图即为拍板**：v1 范围 = C only；C++ 延后 S9 单独裁定（搬/砍）；包边界第一天预留（cpp 报告 5 包草案），两侧都不返工 C 侧。耦合面已证收敛（is_cpp_mode 仅 lexer+parser 最前两层，VM 零感知）。**第七轮亲读补强**：① cpp §10.4 硬事实——**砍 C++ 与 CS2 既定复用策略直接冲突**（`统一整备路线图.md:153,167` 已裁定"CS2 直接复用 C++ 铺路区"），S9 复核必须与 CS 系列计划合并裁定；② cpp §5.4 **短路方案**——无条件 C++ Pass 改显式可组合阶段、空输入短路，"不必砍也能取得'砍'的主要收益"，实际强化 v1=C only 裁决（v1 不搬内容物但保留短路边界） |
| C10 | JIT 归宿三选一 | **倾向性默认：v1 不搬**。依据：①宿主 V8 自带 JIT 边际收益缩水论（评估 §3.1 选项 3）；②⑥-4 溢出语义分歧现存未修 + F1 静默回绕——照抄模板写法则静默错值同时存在两条路径；③统一模式下 JIT 录制纯浪费（unified 新发现）。`jit_path_parity` 八形状+手算期望外置 JSON 作为"复活必全绿"遗产。最终裁定 S9 用门 1 数据复核 |
| C11 | `is_cpp_mode` 计数 43/46/28 | 三口径（全仓 grep/穿透路径/定义点）各自为真；改造点清单以 typeck 逐文件清单为准 |
| C12 | 驱动层语言：scripts 报告 8 包 MoonBit driver-core vs 知识沉淀"Go 驱动无搬迁价值" | **v1 驱动保留 Go**（10,767 行清白资产、D5 刚收官、绞杀者策略新语言只承担引擎本体）；Node 宿主是**新增薄层**（engine-host 接口：spawn/stdin 字节/stdout 逐行/stderr/超时 kill/退出码/RSS 采样）；MoonBit driver-core 作为可选长期演进不进主线 |
| C13 | moon run 可用性（lexer ENOENT vs 管线可用） | 环境差异（node 依赖）；spike 0b 前置检查 |

---

## ④ MoonBit 包切分总图（依赖 DAG，合并 13 份草案）

```
L0 零依赖          vitro/source(SourceLoc+坐标契约)   vitro/opcode(132+Instruction+operand 校验)
                        │
L1 诊断契约        vitro/diag(ErrorCode 137+Severity+SourceLang+Diagnostic+catalog JSON+覆盖率断言)
                        │
L2 抽象语法        vitro/ast(Type/Expr/Stmt/decl+depth+判等渲染单源；不含 compute_type_size)
                        │
L3 名字单源        vitro/names(InstKey→InstId→mangled Name 唯一产出口；parser/typeck 共享最底层)
                        │
L4 前端            vitro/lexer(facade: tokenize→LexResult；internal/{source,pp,host})
                   vitro/parser(↔lexer contract：Token/TokenType 归 lexer，只 re-export)
                   〔预留〕vitro/parser/cpp
                        │
L5 语义            vitro/typeck ── vitro/containers(JSON 数据驱动) ── vitro/libc(单表签名)
                   〔预留〕vitro/typeck/cpp
                        │
L6 发射            vitro/codegen(internal/{layout Planner,frame LIFO 池,c,cpp}+不变量检查器)
                   vitro/bytecode(产物 schema+libc 固定索引+加载+canonical 序列化)
                        │
L7 执行            vitro/memory(载体抽象+MemoryMap+checked_access 单入口+bump/隔离堆+freed_logs 有序结构)
                   vitro/host(110 路由表单源+vfs 入快照；不得直触载体)
                   vitro/vm(executor 穷尽 match 132+snapshot 不可变派生)
                   〔S9 裁定〕vitro/jit(必须可整体移除)
                        │
L8 会话/协议       vitro/session(SessionConfig 值对象+编排)
                   vitro/protocol(帧+schema 版本常量+StepPayload/词汇/契约；不依赖引擎实现)
                   vitro/gateway(wasm-gc 导出 4 函数:invoke/reset/protocol_version/engine_version + NDJSON 主循环)
                        │
L9 教学智能        vitro/time_travel(window/checkpoint/engine)  vitro/teaching/steps
                   vitro/analysis(cfg/algorithms)  vitro/diagnostics(数据包+机制)
                   —— 经 VmObserver/SourceProvider/AlgorithmContext 三接口依赖反转，不依赖 session
                        │
仓库外             Go 驱动层(保留) + Node 宿主 engine-host(新增薄层) + spike 目录
```

**硬约束**（合并各报告）：依赖严格单向无环；`.mbti` 只暴露三层公共面（`protocol` 全量 / `lexer.tokenize` / `typeck.check`），其余 internal；跨包不变量（现 8 条）做成可执行断言；`.mbti` 入版本控制作为协议变更信号——**取代 capi ABI 版本化制度的对外义务载体**；版本承诺锚 `protocol_version` 编译期常量（重锚 replay S5 A4a 的 ping.abi 孤儿语义）。发布形态：`vitro/protocol`、`vitro/lexer`、`vitro/names` 等独立可发布（分片交付对齐 mooncakes）；引擎主交付 = gateway 的 wasm-gc 单产物 + 薄 JS 胶水。

---

## ⑤ 差分对账总方案（分级锚点体系）

| 级 | 锚点 | diff 什么 | 格式/工具 | 前置条件 |
|---|---|---|---|---|
| **A 字节级**（最强） | ①字节码产物 code 段 ②stdout ③最终 1MB 内存映像 | 同源产物逐字节 | Go canonicalizer（键排序/转义统一/缩进固定，**fail loud**）+ 字节 diff | 探针已证可行（3,112 字节逐字节相同，唯一变量 = HashMap 键序 → 排序义务显式继承：**现版确定性完全依赖 Go 侧 sort_keys**，MoonBit 侧原生有序） |
| **B 结构化** | token 流 TSV（L1/L2）、AST dump JSON（E1）、符号表/诊断序列、mangled 名集合、实例化产物 dump、协议帧 NDJSON、error_catalog JSON、标注首现序列 | 解析后结构等价（键集合+值语义，BTreeSet 归一） | Go 驱动；两侧显式 emitter（**禁一侧 serde 派生一侧 derive(ToJson)**——两种派生 JSON 形状必然不同，shared/ast P8 实证风险） | ①E1 依赖 L1 先定列号口径（P6）+ **依赖 P7 的 dump 出口新建**；②E3 依赖 P3 修复；③含非 ASCII/\xHH 用例（现覆盖 0）；④canonicalizer 先埋雷（J9）；⑤**serve 协议补字段级冻结测试**（本轮亲读补入：capi 砍掉后"共用 session_api"锚点消失，出口层 ⑤-1——方案 = serve_smoke 硬编码期望值外置为 `protocol_frames.jsonl` 双宿主对拍）；⑥**白名单补"缺失即红"**（unified M3：`v01_payload_fields.json` 现只判多余不判缺失，"字段只增不改"只守了一半） |
| **C 端到端** | Clang golden 733 `.out` 全量（不抽样）、replay 61 断言、serve_smoke 57 断言、JIT parity 八形状（若 JIT 复活） | stdout 字节 + 退出码 | Go 驱动（判定内核保留）；Node 宿主通道 | golden 缺失必红（P5）；`.out` 只作第二来源，live clang 为主真值 |
| **D 三联 diff**（VM 层专属） | stdout + 返回码 + 1MB 内存映像 | 30 例精选矩阵（VM 报告 §8.3：JIT 五形状+判定性用例+UAF/隔离区+快照往返+字节通道+浮点+调用栈+VFS+路由分叉） | 页级 SHA-256 摘要辅助定位 | 内存 dump 出口新增（`--dump-memory`） |

**三条冻结**（A 级锚成立前提）：①槽位分配策略版本化（v1 逐位兼容现版 / v2 LIFO，二选一显式声明）；②跳转编码（绝对 IP + libc 偏移）；③Bytecode Libc 固定索引段（1000/1024/1089，按"名字→索引"映射比对）。

**节奏**：每搬一个包跑该包锚 + 下游 A/B 级抽检；全量 golden（758 用例）在 VM 片完成时整跑；每锚点上线前有埋雷记录（J9 语言无关义务）。

---

## ⑥ 裸奔期最小防线（重写第一周就必须存在）

采纳 scripts 报告方案（唯一给出可机判完成判据者）：

- **M-0 基线固化**（Rust 版可跑时）：冻结 shadow 报告 + `cases_golden/` 快照 + facts.json，产物入版本控制或固定 tag，clang 版本串记录在报告内。
- **最小防线 = 24~26 例**（scripts 报告候选表全收）：全部来自 baseline、已有 golden、无 stdin；五条选例标准（输出非空格式敏感 / golden 已存在 / 无 stdin / 每语言子域 ≥1 / 不依赖未实现子系统）；必含 `engine_note_lookalike.c`（E-P1-5 口径专用锚）与 `codegen_soundness_regression.c`。
- **三条防假绿纪律**：空集不得绿；golden 缺失必须红；比对必须在字节层。
- **重建里程碑 M-1~M-9**（驱动骨架 → golden 解析 → 全量用例 → live-Clang 主真值 → 三层契约 → fuzz → facts → 台账 CI），每步判据可机判。
- 门 1 spike 的最小 VM 一旦可用，立即扩为 D 级 30 例三联 diff。

---

## ⑦ 差异台账 v0（与 Clang/标准的偏离，机器单源化）

**格式**（知识沉淀报告附录 A 草案采纳入轨）：ID = `DIFF-<域>-<序号>`（域：PTR/LAYOUT/FLOAT/ALLOC/PREPROC/C23/LIB/CPP）；`class` = architectural / implementable / pedagogical；`carry_over` = inherit / fix / drop / retest；数据 = JSON 单源 + 文档生成 + CI 对账（K1~K5 五例文档矛盾的根治）。

**v0 初始条目**（capability_flags **17 项**全量入 JSON——知识沉淀附录 A 示例原数，此前"19 项"系摘要误传；schema 含 `anchors` 与 shadow KNOWN 常量双向对账、`detectable_by_defense` 诚实字段、落地三步含 J9 埋雷，此处列代表项）：

| ID | 内容 | class | carry_over |
|---|---|---|---|
| DIFF-PTR-4BYTE-01 | 指针宽度 4 字节（1MB 线性内存自洽；`_Alignof(int*)` ==4 vs Clang Win64 ==8；锚 function_pointer_sizeof/sizeof_array_param） | architectural | inherit |
| DIFF-LAYOUT-PACKED-01 | struct packed 布局（**防线零覆盖**，U1#13） | architectural | retest（先立 golden） |
| DIFF-FLOAT-LITERAL-01 | 浮点字面量默认 double 语义 / IEEE 精确比较 | architectural | inherit |
| DIFF-LIB-PRINTF-01 | printf 动态宽度 `%*d` 不支持 | implementable | fix |
| DIFF-LIB-FPRINTF-01 | fprintf 到自定义 FILE* 不落盘 | implementable | fix |
| DIFF-LIB-PUTCHAR-01 | `putchar(200)` 非 UTF-8 字节保真（现版输出 2 字节；**A-3 待证 + golden 缺口**） | implementable | fix（Bytes 化顺带根治） |
| DIFF-ALLOC-REALLOC-01 | realloc 总是移动 | architectural | inherit |
| DIFF-ALLOC-QUARANTINE-01 | 有界隔离区 256KB / 窗口外 UAF 漏检诚实记录 | pedagogical | inherit |
| DIFF-PREPROC-STUB-01 | `<>` 只查 14 存根白名单（655 处零迁移） | pedagogical | inherit |
| DIFF-C-LITERAL-01 | 复合字面量生命周期 = 当前块 | pedagogical | inherit |
| DIFF-C-GENERIC-01 | `_Generic` 精确类型匹配（非 C11 完整兼容规则） | pedagogical | inherit |
| DIFF-C-MACRO-SEMI-01 | 参数化宏 + 分号自动 do{}while(0) 包装（Vitro 扩展） | pedagogical | inherit |
| DIFF-PREPROC-MULTILINE-01 | 多行宏 `\` 续行不支持（实测 clang 通过 / Vitro 报 3 条"无法识别的字符"；**规范 §3.1/§9 曾把已实现的 `#`/`##` 与条件表达式一并误写为不支持**——反向漂移，迁移前先修规格否则按错误规格丢能力） | implementable | fix（独立 pass 天然解决） |
| DIFF-TYPE-SHORT-01 | `short`/单 `long` 类型修饰符静默丢弃（lexer 保留 token 但下游语义丢弃，半成品契约） | implementable | fix（或显式 pedagogical 声明） |

C++ 子集 21 条差异（`C++子集规范.md` §4）在 C++ 裁定（S9）时并入。**守门规则**：新引擎行为与台账冲突时，要么改行为要么改台账，禁止沉默漂移（本轮勘察已抓到 2 处规范反向漂移 ⑥-13/14——`#if 'A'==65` 实际支持但规范称不支持，迁移前先修规格）。

---

## ⑧ 风险登记册（A1–A9 对接 + 新增 R1–R10）

| # | 风险 | 触发条件 | 回退动作 | 状态 |
|---|---|---|---|---|
| A2 | wasm-gc 产物不可进 Wasmtime | ~~门 2 红~~ **已证（第五轮，复核记录 §R）：有条件通过**——`-W gc` 下 2.34s 真实运行；v33 CLI 默认拒 GC 模块，部署须显式开特性 | 服务端宿主形态保留 + 部署文档写明开关 | **实测关闭** |
| A3 | 载体吞吐不达标 | ~~门 1 慢 3×~~ **终局（第四轮实测，复核记录 §L-§O）：通过**——真实引擎锚：MoonBit 迷你 VM（wasm-gc/V8）比现役 Rust 解释器**快 3.3×**、比 JIT 慢 2.8×（线内）；第三轮名义红系理想化孪生对照失真（孪生比现役解释器快 23×）；条件 A（S6 性能回归锚）/ 条件 B（LeetCode 全量延迟）随迁 | 门 1 关闭；S6 片起每次跑 1k×1k 等价基准对照 512/55ms 双基线 | **实测关闭·方向有利** |
| A5 | 不可变共享不能无代价替代 Arc | H2 红 | heatmap 分片重设计 / OutputLog 分块 | 待证（VM 报告 §1.9 已证"实际语义被高估"） |
| A6 | MoonBit breaking change | 1.0 后频率不可接受（v0.10.4 已移除包格式；F8 ICE 实证不稳） | 依赖面收窄：只用 core 稳定子集；升级走双版本产物 diff | 持续监控 |
| A7 | LLM 效率不足 | ~~门 0 红~~ **实测关闭（第六轮，复核记录 §U）**：干净上下文首过 1/6 但每函数 ≤2 轮收敛、6/6 正确（含 quirk 保真）；三大语言差异进 S1 工程约定；遗留 = 无同协议 Rust 对照组 | 重写按"每函数预期 1~2 轮编译迭代"排期 | **关闭·可支撑** |
| A8 | golden 跨实现复用 | — | 已证成立（推理+实测交叉） | ✅ |
| R1 | F1 静默回绕 × 教学溢出 trap 语义冲突 | 全链未显式 checked | checked 纪律写进包规格；检测成本入 1c | 实证在册 |
| R2 | HashMap 跨宿主种子差异（F6）——**本轮亲读强化：主战场恰是开发期本地通道**（core 源码取证：`Hasher` 种子 wasm/wasm-gc=0 而 native/llvm/js=随机；`HashMap::iter` 文档明写 unspecified order）⇒ 差分对账在不同宿主会得出不同键序，"wasm-gc 恰好确定"不能救人——本地开发通道就在随机侧 | 任何产物/序列化依赖迭代序（历史实锤：Bytecode Libc 产物布局不可重现，修复=Go 侧 sort_keys） | 全部输出显式排序 / LinkedHashMap；"同输入两次运行产物哈希相同"入 CI；**排序义务显式继承**（现版产物确定性完全依赖 Go 侧 `sort_keys`，typeck ⑥-11 + VM 附录 A-4 双报告确认） | 实证在册 |
| R3 | UTF-16 三单位混用（F2） | 任何 `length()` 参与字节数计算 | 坐标口径契约进 `vitro/source`；code review 禁令 + spike 断言 | 实证在册 |
| R4 | JIT 语义分歧带病迁移（⑥-4） | 若搬 JIT 且照抄模板 | C10 倾向不搬；复活必先修分歧 + parity 全绿 | 已裁决倾向 |
| R5 | 双头维护拖累 | Rust 侧新特性分叉 | 冻结纪律：Rust 侧只收安全修复，新特性一律 MoonBit 侧 | 制度化 |
| R6 | 裸奔期静默错值 | 防线重建窗口 | §⑥ 最小防线 + M-0 基线 | 已设计 |
| R7 | 生成物/手写物无门禁（opcode 注释 128vs137、libc index 只 stat） | 再发生漂移 | 生成期校验"源变则产物变"入 CI | 登记 |
| R8 | 大赛/生态窗口错过 | MoonBit 1.0 当月启动重写遇 breaking | 分片发布节奏不受单点窗口绑架；每季度检查押注兑现度 | 制度化 |
| R9 | 76,218→重写成本随时间上涨 | 拖延 | 分片推进，每片独立可验收；锚点体系保证中途可停可续 | 已设计 |
| R10 | 驱动链重建期防线能力降级（RSS 口径等） | Node 宿主形态 | RSS 护栏判据重定义（进程级→实例级）；列入"防线能力降级"显式台账 | 登记 |
| **R11** | **wasm 冒烟证据链断在文档**（出口层 ⑥-9/⑥-10 实测：`wasm_smoke.js:45` 的 ABI 断言是**空洞断言**——`vitro_abi_version()` 返回堆指针 i32，断言实际测"指针非空"却贴"ABI 版本"标签；3.75MB/3.44MB/磁盘 3,602,162B 三方不符且无机器断言；磁盘产物是更名前 `cide_native.wasm`；UAF/Double-Free 在 wasm 下的证据源是**未入库**的 %TEMP% 手工脚本） | 迁移期不补则新出口继承一个没有防线的"working"声明 | 重建 wasm 冒烟：真 E3070/E3061 断言 + 体积断言 + 产物更名；**新出口的每条护栏断言必须先证明它会红**（出口层 ⑥-9 点名违反红→绿纪律第 3 条） | 实证在册 |
| **R12** | 引擎 `rand()` 默认种子依赖系统熵（wasm-gc **无熵源**：`@env.rand_internal` 直接 `return false`，F5/scripts S2 双实证） | 新引擎在 wasm-gc 目标下 rand 行为分叉 | 默认种子取确定值（现版依赖 `session.runtime.rand_seed` 初值）；`srand_rand.c` golden 刻意零数值依赖的设计**原样保留** | 实证在册 |

---

## ⑨ 里程碑切片（绞杀者节奏：每片 = 重写进度 + mooncakes 发布 + 押注检查点）

| 片 | 内容 | 验收判据（引用 §⑤ 锚点级别） | 发布 |
|---|---|---|---|
| S0 门阶段（1~2 周） | ②全部 ★ 项 + 0b/0c | 门 0/1/2/3 判定书（含冲突消解结论） | 不发布（仓库外 spike） |
| S0.5 Rust 止血批（并行） | §3.1 P1~P6 + M-0 基线冻结 | 每条红→绿留痕；基线快照入版本控制 | — |
| S1 基础片 | `vitro/{source,diag,opcode,ast}` | E1 AST dump（B 级）+ error_catalog JSON（B 级）+ 码表生成幂等 | `vitro/diag` 首发 mooncakes |
| S2 词法片 | `vitro/lexer`（预处理独立 pass + LineMap + 宿主 IO 抽象） | L1/L2 token TSV（B 级）+ 随机词法差分 ≥2000 例（**超额完成既有反证义务**；载体建议 `.mbtx` 语料生成器——lexer ⑥-25：三次 lexer 大规模止血全部来自人工构造用例，无 fuzz 就没有新形状输入） | `vitro/lexer` |
| S3 语法片 | `vitro/parser`（深度预算统一入口、J1 语义不复刻旧防护） | E1/E2 诊断序列 + E3 病态输入"同等拒绝"（12 崩溃样本）+ E4 合法程序反向锚 + **解析器活性内部断言**（本轮亲读补入：GC 下死循环表现为堆增长+GC 压力而非 RSS 单调上升，故障信号更弱——"解析 N token 后 pos 严格递增"必须做成内部断言，parser 6-3） | — |
| S4 语义片 | `vitro/{names,typeck,containers,libc}` | E1~E4 + mangled 名集合相等（U3#6 验收）+ 符号表快照 | `vitro/names` |
| S5 发射片 | `vitro/{codegen,bytecode}` | **A 级**产物 code 段逐字节 + libc 自举 12 用例 + LIFO 八条事故回归 + r1 7 道 + **A 级前提的排序义务显式继承**（本批亲读复核：现版产物确定性完全依赖 Go 侧 `sort_keys`——`vitro_cli.rs:308` 直接 serde HashMap，键序随进程随机；MoonBit 侧必须原生输出有序结构，typeck ⑥-11 与 VM 附录 A-4 双报告确认）+ **`--dump-compile-output` 工具新建**（codegen L4：source_map/symbols/struct_defs/union_defs/global_data_end 五字段不进产物，当前无法 diff）+ **codegen 生成本体自建单测层**（现状 crate 内仅 11 个工具函数测试、本体零单测，codegen §⑨） | — |
| S6 执行片 | `vitro/{memory,host,vm}` | **D 级** 30 例三联 diff + 门 3 全字段 + UAF/隔离区红锚全绿 | `vitro/vm`（或合并 core） |
| S7 会话片 | `vitro/{session,protocol,gateway}` + Node 宿主 | 协议帧 NDJSON（B 级）双宿主对拍 + replay 61 / serve_smoke 57 重建 | `vitro/protocol` + wasm-gc 产物 |
| S8 白箱片 | `vitro/{time_travel,teaching,analysis,diagnostics}` + 差异台账机器单源化 | seek 往返五类快照相等 + 标注 golden 311 条三方 diff + 台账 CI 对账；**前置（第七轮亲读补入）**：认知链二/三/四层与补全在 Rust 侧零出口（spike 表 U1）——先补最小导出否则本片迁移无等价性证据；diagnostics 报告建议 algorithms+steps 两包（bug 密度最高区）放迁移后期并与"执行事件重构"绑定 | 认知链切片 |
| S9 裁定批（阶段 2） | JIT 存废（C10 倾向不搬，用门 1 数据复核）/ C++ 搬或砍（包边界已预留）/ Bytecode Libc 机制形态 / Wasmtime 形态 | 各自独立判定书；jit_path_parity 遗产 JSON 备查 | — |
| 全量切换 | C 侧 758 用例 + golden 733 全绿 + facts 制度双轨收口 → Rust 侧退役为归档 | shadow 报告逐项一致（match/known_issue/gap 三口径） | 1.0 |

**节奏纪律**：锚点未全绿不发版（typeck 报告发布规则采纳）；每片完成即回填 facts 真值键（新引擎侧键空间独立）；大赛开始时以"四门判定书 + 已发布切片 + 过程博客"进场。**任一时刻可停**：锚点体系保证每片独立可验收，中途冻结不损失已落地资产（对应 R9）。

---

## 附录 · 本蓝图的裁定权限声明

本蓝图对 C1~C13 十三处冲突给出裁决、对 C9（C++ v1 排除）与 C10（JIT 倾向不搬）给出**倾向性默认**，均附依据引用；其中 C9/C10 的最终确认权在项目所有者，S9 复核点前均可推翻（推翻成本已通过包边界预留与遗产 JSON 压到最低）。蓝图本身为活文档：每片完成回填状态，重大偏离须留痕（诚实记录制度）。
